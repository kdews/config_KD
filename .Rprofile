# # Use pre-compiled binaries for package installation
# options(HTTPUserAgent = sprintf("R/%s R (%s)",
#                                 getRversion(),
#                                 paste(getRversion(),
#                                       R.version["platform"],
#                                       R.version["arch"],
#                                       R.version["os"])),
#         repos = "https://packagemanager.rstudio.com/all/__linux__/centos7/latest")

# Use showtext font database (if installed)
if (require(showtext, quietly = TRUE)) {
      showtext_auto()
}

# Default setting of stringsAsFactors = FALSE
options(stringsAsFactors=FALSE)

# Force R to output numbers without scientific notations
options(scipen=10)
